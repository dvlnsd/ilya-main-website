const body=document.body,menu=document.querySelector('.mobile-menu'),toggle=document.querySelector('.menu-toggle'),panel=document.querySelector('.contact-panel'),overlay=document.querySelector('.overlay');
function menuState(open){if(!menu||!toggle)return;menu.classList.toggle('active',open);menu.setAttribute('aria-hidden',String(!open));toggle.setAttribute('aria-expanded',String(open));body.classList.toggle('lock',open)}
function panelState(open){if(!panel||!overlay)return;panel.classList.toggle('active',open);overlay.classList.toggle('active',open);panel.setAttribute('aria-hidden',String(!open));body.classList.toggle('lock',open);if(open)menuState(false)}
toggle?.addEventListener('click',()=>menuState(!menu.classList.contains('active')));document.querySelectorAll('[data-contact-open]').forEach(b=>b.addEventListener('click',()=>panelState(true)));document.querySelectorAll('[data-contact-close]').forEach(b=>b.addEventListener('click',()=>panelState(false)));overlay?.addEventListener('click',()=>panelState(false));document.addEventListener('keydown',e=>{if(e.key==='Escape'){menuState(false);panelState(false)}});
